#include <stdio.h>
#include <stdlib.h>

typedef struct lista{
	char *letra;
	struct lista *prox;
}node;

node *insere(node *prox, char *letras);

void main()
{
	char *letra;
	letra = (node*)malloc(sizeof(node));
}

node *insere(node *prox, char *letras)
{
	node novoNO;
	novoNO = NULL;
	
	
	
}
