#include <stdio.h>
#include <stdlib.h>

typedef struct lista{
	char letra;
	struct lista *prox;
}node;

inverte_lista(node LISTA, char frase[20]);

inverte_lista(node LISTA, char frase[20]){
	
}

void main ()
{
	char frase[20] = "ISSO E UM TEXTE.";
	
	
	
}
